```bash
sudo pacman -S hyprland kitty noto-fonts
```

`TODO(Frans): Figure out what is actually the best choice for firefox and dolphin :)`
```bash
sudo pacman -S firefox # choose 2, pipewire-jack
sudo pacman -S nautlius gvfs-smb # gvfs-smb allows for smb shares
sudo pacman -S hyprlauncher
```